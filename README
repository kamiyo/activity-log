TODO

Remove material-table
Use paginated stickyheader table
Floating button for adding
On click row, change button to edit
Use dialog for edit

Serverside stuff
Should use psql (can't guarantee functionality of others)

Need node-gyp on server (global install) for hashing and GCC.
once build, use node ./server/build/createUser.js [username] [password] to create user.