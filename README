# Activity Log
## For your baby (or you)

Built with React and Material-UI. Backend with Express and Sequelize
Use PostgreSQL or else I have no idea what will happen.

node-gyp is needed on the server (global install) in order to install and use Argon2 for password hashing.
once built, use node ./server/build/createUser.js [username] [password] to create user.